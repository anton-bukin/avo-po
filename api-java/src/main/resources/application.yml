server:
  port: 3100

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pspay
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  jwt:
    secret: pspay-demo-secret-key-2026-java-spring-boot
    expiration-ms: 2592000000  # 30 days
  admin:
    user-id: 1
